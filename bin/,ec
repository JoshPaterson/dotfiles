#!/bin/bash
#
# Quick access to config files so I don't have to remember where they all live

e=nvim

while getopts ":h" option; do
 case $option in
  h)
    echo 'bashrc'
    echo 'bash_profile'
    echo 'awesome'    
    echo 'kitty'
    echo 'vifm'
    echo 'vim'
    echo 'rofi'
    echo 'neofetch'
    echo 'neovim'
    echo 'ranger'
    echo 'rifle'
    echo 'zathura'
    echo 'starship'
    echo 'qtile'
    exit 0;;
 esac
done

case $1 in
  bashrc)
    $e ~/.bashrc
    ;;
  bash_profile)
    $e ~/.bash_profile
    ;;
  awesome)
    $e ~/.config/awesome/rc.lua
    ;;
  kitty)
    $e ~/.config/kitty/kitty.conf
    ;;
  vifm)
    $e ~/.config/vifm/vifmrc
    ;;
  vim)
    $e ~/.vimrc
    ;;
  rofi)
    $e ~/.config/rofi/config.rasi
    ;;
  neofetch)
    $e ~/.config/neofetch/config.conf
    ;;
  neovim)
    $e ~/.config/nvim/init.vim
    ;;
  ranger)
    $e ~/.config/ranger/rc.conf
    ;;
  rifle)
    $e ~/.config/ranger/rifle.conf
    ;;
  zathura)
    $e ~/.config/zathura/zathurarc
    ;;
  starship)
    $e ~/.config/starship.toml
    ;;
  qtile)
    $e ~/.config/qtile/config.py
    ;;
esac

